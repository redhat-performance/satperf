---
# Example: Replace ReX execution with concurrent_execution.yaml
#
# OLD ReX approach:
#   test="65f-rex-ansible-podman-pull-${num_matching_rex_hosts}"
#   apj $test \
#     -e "description_format='${num_matching_rex_hosts} hosts - %{template_name}: %{command}'" \
#     -e "job_template='$job_template_ansible_default'" \
#     -e "search_query='$search_query'" \
#     -e "command='bash -x /root/podman-login.sh && bash -x /root/podman-pull-rhosp.sh'" \
#     -e "task_timeout=$(( num_matching_rex_hosts < 450 ? 450 : num_matching_rex_hosts ))" \
#     playbooks/tests/FAM/job_invocation_create.yaml
#   ejji $test
#
# NEW concurrent_execution approach:
#   test="65f-concurrent-podman-pull-${concurrent_executions}"
#   ap $test.log \
#     -e "size='$concurrent_executions_per_container_host'" \
#     -e "script='bash -x /root/podman-login.sh && bash -x /root/podman-pull-rhosp.sh'" \
#     playbooks/tests/concurrent_execution.yaml
#   e Execute "$logs/$test.log"
#
# Benefits:
# - No ReX/Sidekiq/Dynflow overhead
# - Direct execution via Ansible SSH
# - Faster startup time
# - Same timing and status collection
...
