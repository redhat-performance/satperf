---
# Satellite first: capsule restore redeploy needs the Satellite API up.
- name: Satellite backup and restore test
  hosts: satellite6
  gather_facts: false
  tasks:
    - ansible.builtin.include_tasks: includes/run-backup-test.yaml

- name: Capsule backup and restore test
  hosts: capsules
  gather_facts: false
  tasks:
    - ansible.builtin.include_tasks: includes/run-backup-test.yaml
