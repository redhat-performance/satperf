---
- name: "Manage repositories"
  hosts: localhost
  gather_facts: false

  roles:
    - role: theforeman.foreman.repositories
      vars:
        foreman_products: "{{ products | from_yaml if products is string else products }}"
...
