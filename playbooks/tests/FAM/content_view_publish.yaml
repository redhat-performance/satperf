---
- name: "Publish content views"
  hosts: localhost
  gather_facts: false

  roles:
    - role: theforeman.foreman.content_view_publish
      vars:
        foreman_content_views: "{{ content_views | from_yaml if content_views is string else content_views }}"
...
