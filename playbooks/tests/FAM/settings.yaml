---
- name: "Configure Foreman settings"
  hosts: localhost
  gather_facts: false

  roles:
    - role: theforeman.foreman.settings
      vars:
        foreman_settings: "{{ settings | from_yaml if settings is string else settings }}"
...
