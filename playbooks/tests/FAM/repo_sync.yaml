---
- name: "Sync repository"
  hosts: localhost
  gather_facts: false

  module_defaults:
    group/theforeman.foreman.foreman:
      server_url: "{{ foreman_server_url | default(omit) }}"
      username: "{{ foreman_username | default(omit) }}"
      password: "{{ foreman_password | default(omit) }}"
      validate_certs: "{{ foreman_validate_certs | default(omit) }}"
      organization: "{{ foreman_organization }}"

  tasks:
    - name: Product
      when:
        - product is defined and product | length > 0
      vars:
        _task_name: "ProductSync"
      block:
        - name: "{{ _task_name }}"
          theforeman.foreman.repository_sync:
            product: "{{ product }}"
            repository: "{{ repository | default(omit) }}"
          async: 14400
          poll: 0
          register: __fire_result

        - ansible.builtin.include_tasks: includes/fam_wait_and_print.yaml
          vars:
            _async_jid: "{{ __fire_result.ansible_job_id }}"
...
