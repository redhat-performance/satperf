---
- hosts: docker_hosts
  remote_user: root
  gather_facts: no
  vars_files:
    - ../../conf/satperf.yaml
    - ../../conf/satperf.local.yaml
  tasks:
    - name: "Clean test-folder for download test"
      shell: rm -rf /test-folder/*

